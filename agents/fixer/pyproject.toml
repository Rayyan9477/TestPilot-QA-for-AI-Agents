[project]
name = "testpilot-fixer"
version = "0.1.0"
description = "TestPilot Fixer coded agent (UiPath AgentHack, Track 3)"
authors = [{ name = "TestPilot authors" }]
dependencies = ["uipath>=2.10.0,<2.11.0", "pydantic>=2"]
requires-python = ">=3.11"
